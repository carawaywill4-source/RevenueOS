{
  "name": "@revenueos/mission-watchdog",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Deterministic external watchdog: keeps the RevenueOS commercial mission alive even when the operator crashes or a lane idles. Separate systemd unit. No LLM in the loop.",
  "main": "src/index.ts",
  "scripts": {
    "start": "tsx src/index.ts",
    "test": "tsx --test src/tests/*.test.ts"
  },
  "dependencies": {
    "@revenueos/core": "file:../../packages/revenueos",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/pg": "^8.15.5",
    "tsx": "^4.23.10",
    "typescript": "^5"
  }
}
