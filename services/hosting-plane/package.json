{
  "name": "@revenueos/hosting-plane",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "RevenueOS hosting plane — deploys/runs portfolio storefronts independently of Vercel and of RevenueOSCore.",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "tsx --test src/**/*.test.ts"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.109.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^20",
    "tsx": "^4.23.10",
    "typescript": "^5"
  }
}
